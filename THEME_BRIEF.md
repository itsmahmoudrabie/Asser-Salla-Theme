# برييف بناء ثيم «آسر» الكامل — مرجع Claude Code

> هدف الملف: يبقى المرجع الوحيد اللي Claude Code يرجعله في أي جلسة عشان مايضيعش أي قرار. احتفظ بيه في جذر مشروع الثيم باسم `THEME_BRIEF.md`.

---

## 1) الهوية

| العنصر | القيمة |
|---|---|
| اسم الثيم | **آسر** (aser) |
| الصناعة الأساسية | الأزياء والعطور والجمال (الديكور صناعة ثانوية) |
| الاتجاه الفني | Luxury Minimal — فخامة هادية، مساحات بيضاء واسعة، حركة ناعمة بطيئة |
| المنافس المرجعي | ساجي (+210 إعداد، أداء) — والتفوق البصري على سيليا شرط موازٍ |
| الرؤية | جمال سيليا البصري + أرقام ساجي في الأداء والإعدادات، وأعلى من الاتنين |

## 2) نظام الألوان (CSS Variables — إلزامي)

```css
:root {
  --aser-ivory: #F7F3EC;      /* خلفية أساسية */
  --aser-charcoal: #2B2926;   /* نص أساسي / وضع داكن قياسي */
  --aser-gold: #B08D57;       /* Accent أساسي — بطل الهوية */
  --aser-navy: #1B2A4A;       /* Accent ثانٍ — بانرات، فوتر، شارات */
  --aser-navy-dark: #12203A;  /* خلفية داكنة بديلة لأقسام مميزة */
  --aser-sand: #E9DFC9;       /* سطح ثانوي — كروت وأقسام بديلة */
}
```

قاعدة الاستخدام: الذهبي بطل الهوية دايمًا. الكحلي لمسة ثانوية ≤ 15% من أي شاشة. كل الألوان لازم تكون قابلة للتعديل من لوحة التاجر (Theme Settings في twilight.json)، مش hardcoded.

## 3) التايبوغرافي

- **العناوين:** خط عربي Serif فاخر (Amiri أو مكافئ) — لكل H1/H2/H3 وعناوين السكاشن.
- **النصوص:** خط Sans نظيف (IBM Plex Arabic أو Rubik) — للفقرات والعناصر التفاعلية.
- خط لاتيني مكمّل لو احتجنا (أسماء براندات إنجليزي) بنفس روح Serif/Sans.

## 4) المتطلبات التقنية غير القابلة للتفاوض

- Lighthouse موبايل ≥ 90 (رئيسية + منتج).
- عدد إعدادات لوحة تاجر مستهدف: **220+** (مرجع ساجي = 210).
- RTL كامل بدون استثناء.
- وضع داكن كامل (مش سكشن بسكشن) — الكحلي الغامق `--aser-navy-dark` بديل مقترح للفحمي في أقسام مختارة بالوضع الداكن.
- كل سكشن قابل للتخصيص/الإخفاء من لوحة التاجر بدون كود.
- Sticky elements وLazy loading لكل الصور.

## 5) صفحة الرئيسية — قائمة السكاشن الكاملة (12-15 سكشن)

### أ) تكافؤ مع سيليا (إلزامي — نفس المستوى أو أعلى)
1. سلايدر رئيسي متقدم (طبقات نصوص + أزرار متعددة + دعم فيديو خلفية)
2. بانر مزدوج
3. بانر ثلاثي
4. بانرات متحركة (Marquee/Auto-scroll)
5. بانر ديناميكي (يتغير حسب شرط: عرض / تصنيف / وقت)
6. تبويبات منتجات (Tabs: الأحدث / الأكثر مبيعًا / عروض)
7. منتجات مخصصة يدويًا
8. معرض فيديو
9. قبل وبعد (Before/After slider)
10. آراء عملاء مخصصة (Custom testimonials block)
11. روابط مربعة/دائرية/مخصصة (Category shortcuts)
12. سكشن "عن المتجر" (Brand story)
13. فاصل بصري (Divider) بين السكاشن

### ب) ميزات التفوق (نقاط بيع «آسر» الخمس — أولوية قصوى)
14. **ريلز قابلة للتسوق (Shoppable Reels)** — فيديوهات قصيرة عمودية بنقاط منتجات قابلة للنقر، تصميم يحاكي TikTok/Reels.
15. **Shop the Look** — صورة لايف ستايل واحدة بنقاط hotspot تفتح كارت منتج مصغر.
16. عداد تنازلي للعروض (Countdown) + عداد مخزون منخفض (Scarcity) — يتكاملوا مع سكشن العروض.
17. شريط إعلانات متحرك أعلى الصفحة (Announcement bar) — رسائل متعددة متبدلة.
18. أسئلة شائعة (FAQ) بشكل أكورديون.

> ملحوظة: لو العدد الكلي زاد عن 18، ادمج (12) و(13) في سكشن واحد مرن، أو اجعل الفاصل (13) عنصر تصميم عام مش سكشن منفصل في القائمة.

### ج) عناصر عامة عبر الصفحة (مش سكاشن منفصلة، لازم تتوفر في كل مكان مناسب)
- سلة جانبية سريعة (Drawer cart) مع Upsell — **من نقاط البيع الخمس**.
- Sticky Add-to-Cart على الموبايل — **من نقاط البيع الخمس**، ويظهر في صفحة المنتج بالأساس.
- بحث فوري (Instant search) في الهيدر.
- سكشن إنستجرام/UGC (اختياري لو المساحة سمحت ضمن الـ 15 سكشن).

## 6) صفحة المنتج

- معرض صور متقدم (Zoom + دعم فيديو).
- **Sticky Add-to-Cart للموبايل** (نقطة بيع أساسية).
- شريط ثقة (شحن / دفع آمن / استبدال).
- **عداد مخزون منخفض** (نقطة بيع أساسية).
- منتجات مكملة (Cross-sell).
- اشتري معاه (Bundle) إن أمكن ضمن الوقت المتاح.
- تقييمات العملاء بشكل بصري مميز (مش جدول عادي).

## 7) الصفحات الإلزامية الأخرى

- صفحة التصنيفات/المتجر: فلاتر سريعة، Grid منتجات متجاوب.
- صفحة السلة: عرض واضح + upsell.
- صفحات العميل: الملف الشخصي، الطلبات، المفضلة، الإشعارات.
- المدونة والصفحات العامة (من نص/سياسات).

## 8) خارج النطاق للإصدار الأول (ممنوع الدخول فيه)

Page builder مخصص — تعدد لغات كامل — سكاشن زيادة عن 15 (باستثناء ما هو مذكور أعلاه) — تطبيقات مصاحبة. كل ده للإصدار 1.1 بعد الإطلاق.

## 9) ترتيب البناء المقترح لـ Claude Code

1. Design tokens (CSS Variables + خطوط) في ملف أساسي واحد يتستدعى من كل السكاشن.
2. الهيدر والفوتر (شريط الإعلانات + بحث فوري + سلة جانبية جوه الهيدر).
3. سكاشن الرئيسية بالترتيب في القسم 5 (أ) الأول، بعدين (ب).
4. صفحة المنتج كاملة.
5. صفحة التصنيفات والسلة.
6. صفحات العميل والمدونة.
7. مراجعة الأداء (Lighthouse) وتعديل الصور/الـ lazy loading.
8. مراجعة عدد الإعدادات في twilight.json مقابل هدف 220+.

## 11) الأساس التقني الحقيقي (مؤكد من كود Theme Raed الرسمي)

> اتأكد ده فعليًا بفحص https://github.com/SallaApp/theme-raed — النسخة اللي بتتنزلك تلقائيًا عند `salla theme create`.

### أدوات البناء
`pnpm` (مش npm) + `webpack` + `Tailwind CSS` + `SCSS`. Node مطلوب `^22.18` أو `>=24.11`.

### مكان الألوان والخطوط
الملف: `src/assets/styles/01-settings/global.scss`

```scss
:root {
  --color-primary: #B08D57;        /* يتحدد فعليًا من Partners Portal (خاصية color) */
  --color-primary-d: #8A6D42;
  --color-primary-l: #C7A876;
  --color-primary-reverse: #FFFFFF;
  --main-text-color: #2B2926;      /* الفحمي */
  --color-text: #4A453F;
  --font-main: "Amiri";

  /* إضافات آسر المخصصة (مش من القالب الأصلي) */
  --color-ivory: #F7F3EC;
  --color-sand: #E9DFC9;
  --color-navy: #1B2A4A;
  --color-navy-dark: #12203A;
}
```

وفي `src/views/layouts/master.twig` الألوان بتتحقن فعليًا كده (لازم Claude Code يحافظ على نفس النمط ده مش يخترع نمط جديد):

```twig
<style>
    :root {
        --font-main: '{{ theme.font.name }}';
        --color-primary: {{ theme.color.primary }};
        --color-primary-dark: {{ theme.color.darker(0.15) }};
        --color-primary-light: {{ theme.color.lighter(0.15) }};
        --color-primary-reverse: {{ theme.color.reverse_text }};
        --color-navy: {{ theme.settings.get('color_navy', '#1B2A4A') }};
    }
</style>
```

الكحلي مش لون أساسي في نظام سلة، فلازم يتضاف كـ setting مخصص جديد في `twilight.json` بصيغة `"format": "color"` تحت الـ settings، ويتحقن بنفس طريقة `theme.settings.get()`.

### مكونات الرئيسية الجاهزة فعليًا (موجودة في `src/views/components/home/`)

`enhanced-slider` (السلايدر) — `enhanced-square-banners` (بانر مزدوج/ثلاثي) — `slider-products-with-header` (تبويبات منتجات) — `products-slider` — `featured-products-style1/2/3` — `testimonials` + `custom-testimonials` — `youtube` (معرض فيديو) — `square-photos` (روابط مربعة) — `store-features` (عن المتجر) — `brands` — `fixed-banner` (بانر ديناميكي) — `fixed-products` — `parallax-background` — `latest-products` — `main-links`.

**قرار مهم لـ Claude Code:** الشغل الأساسي على السكاشن دي هو **إعادة تصميم بصري (Tailwind classes + SCSS) بهوية Luxury Minimal**، مش إعادة بناء المنطق البرمجي من الصفر. الصفحة الرئيسية (index.twig) بترندر بس بـ:

```twig
{% extends "layouts.master" %}
{% block content %}
    {% component home %}
{% endblock %}
```

### مكونات لازم تُبنى من الصفر (مش موجودة في القالب الأصلي)
ريلز قابلة للتسوق — Shop the Look — Before/After — FAQ أكورديون — عداد تنازلي (Countdown، لو مش مغطى بميزة جاهزة). تتبنى كملفات جديدة داخل `src/views/components/home/` وتُسجّل في `twilight.json` تحت `components` بنفس صيغة المثال في القسم 9 من توثيق Salla (name/title/icon/path/fields).

## 12) مراجع تقنية مباشرة

- الثيم المرجعي الرسمي (نقطة البداية الفعلية): https://github.com/SallaApp/theme-raed
- توثيق twilight.json: https://docs.salla.dev/421921m0
- توثيق CSS Variables: https://docs.salla.dev/421945m0
- توثيق Custom Fonts (لازم تُراجع قبل تفعيل خط Amiri لأن قائمة الخطوط الافتراضية محدودة): https://docs.salla.dev/422551m0
- توثيق Master Layout: https://docs.salla.dev/421944m0


